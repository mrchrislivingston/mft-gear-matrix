import 'package:flutter_test/flutter_test.dart';
import 'package:mft_gear_matrix/main.dart';

void main() {
  test('GearMatrixApp can be created', () {
    const app = GearMatrixApp();

    expect(app, isA<GearMatrixApp>());
  });
}